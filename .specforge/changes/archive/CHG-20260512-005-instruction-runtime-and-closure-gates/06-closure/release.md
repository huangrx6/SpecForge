# 发布记录

## 发布摘要

本次变更完成 SpecForge v0.1 运行时闭环增强：

- 新增 `instructions`、`gate`、`archive` 命令。
- 将 `closure` 纳入 `standard` workflow。
- 增强结构校验和 registry 生命周期校验。
- 完成 CHG-005 自举验证。

## 部署说明

当前项目未发布 npm 包，部署方式为仓库内文件更新。使用者拉取仓库后即可通过 npm scripts 运行。

## 发布状态

Ready for archive.
