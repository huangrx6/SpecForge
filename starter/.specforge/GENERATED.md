# 生成产物

此目录由 `core/starter.manifest.json` 生成，不是源码母本。

不要手工修改本目录内容。需要更新 starter 时，修改 `core/` 母本或 `core/starter.manifest.json`，然后运行：

```bash
node core/scripts/sync-starter.mjs
node core/scripts/sync-starter.mjs --check
```
