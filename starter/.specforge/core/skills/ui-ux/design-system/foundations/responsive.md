# Responsive

## 断点策略

| 断点 | 用途 |
|---|---|
| mobile | 单列、底部操作、触控优先 |
| tablet | 双列或折叠侧栏 |
| desktop | 主工作区、表格、筛选、批量操作 |
| wide | 大屏 dashboard、监控、数据对比 |

## Product UI

- 表格在窄屏要有列裁剪、卡片化或横向滚动策略。
- 主操作不能因为断点隐藏到不可发现的位置。
- 筛选区折叠后要显示已选条件数量。

## 输出

记录每个关键页面在 mobile / desktop / wide 的布局变化。
