# Traceability

Deprecated compatibility entry.

本文件保留为兼容入口。完整执行规则已经合并到 `behavior-contract.md#4-traceability`。

读取本文件时，继续读取 `behavior-contract.md`，并使用其中的 Source -> REQ -> AC -> Downstream 链路、trace rules 和 examples。
