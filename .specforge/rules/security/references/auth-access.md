# 认证、授权和访问控制

## 核心区分

- 认证回答“你是谁”。
- 授权回答“你能做什么”。

设计中必须分开描述。

## 基本规则

- 后端必须做权限校验。
- 前端菜单或按钮隐藏不能作为唯一保护。
- 权限判断应基于可信身份、角色、资源 owner 和数据范围。
- 默认拒绝，显式允许。
- 跨租户、跨组织、管理员角色和批量操作必须有额外验证。
- token 应有过期时间、撤销策略和作用域。

## Review Checklist

- 是否只做了前端权限。
- 是否存在跨租户越权风险。
- token 生命周期是否明确。
- 关键操作是否有审计。
