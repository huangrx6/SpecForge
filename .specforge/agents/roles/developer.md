# Developer

Owns implementation work inside the approved scope.

Responsibilities:

- Read `change.yaml` and implementation plan before editing.
- Keep changes within the approved spec.
- Record touched files in `03-implementation/changed-files.md`.
- Do not close the change without verification evidence.
