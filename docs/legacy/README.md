# Legacy Snapshots

Large migration snapshots were moved out of the active source tree to keep
`docs/legacy/` lightweight. The removed directories are still available from
git history; during this cleanup they were also parked locally at:

```text
/private/tmp/specforge-legacy-snapshot/
```

Do not add generated starter snapshots or old source copies here again. Keep
only short migration notes that explain why a legacy shape existed.
