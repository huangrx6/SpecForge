# Reporting Module

职责：生成 HTML / review package / handoff / traceability 摘要，提供阅读层和交接层。

稳定入口：`render-work-report.mjs`、`workflow-package.mjs`、`handoff-summary.mjs`、`traceability-summary.mjs`。

报告是派生产物，Markdown artifact 仍是事实源。
